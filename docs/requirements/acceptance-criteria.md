# Acceptance Criteria

## Interpretation

Requirement IDs are stable references for implementation issues, tests,
reports, and release evidence. A target evidence level describes the minimum
proof needed before an item can be accepted; it does not upgrade current
evidence. A future owning contract must define its exact diagnostic code before
runtime implementation. Existing `LW_EVAL_*` diagnostics remain authoritative
only where already defined by the EvaluationSpec contract.

| ID | Story / priority | Preconditions | Normal path and observable result | Negative path and required disposition | Target evidence / test layer | Current evidence and blocker |
| --- | --- | --- | --- | --- | --- | --- |
| AC-01 | US-01 / P0 | Teacher material is identified; approved Agent and policy bindings are configured. | Candidate records assumptions, input identity, schema version, explicit runtime choice, risk list, and validation result; only approved candidate may publish. | Missing material/version, unsafe dependency, absent binding, validation failure, or missing approval blocks publish with a sanitized stable diagnostic. | connected contract, integration, policy, and teacher E2E. | implemented; current-head material upload, Claude Code execution and independent approval replay are pending. |
| AC-02 | US-02 / P0 | Published immutable template and eligible actor exist. | Same declared template revision reaches observed `Ready`; endpoint health, revision, and allowed retained state are observable. | Revision conflict, non-Ready/unhealthy endpoint, or cleanup failure denies access or transitions to explicit failure; no partial success. | connected lifecycle/idempotency, Access integration, and student E2E. | Container/KubeVirt implementations and an older deployment exist; current-head real dual-runtime replay is pending. |
| AC-03 | US-03 / deferred | Approved EvaluationSpec and immutable submission are available. | Future deterministic steps produce versioned evidence and aggregate according to an approved spec. Sprint 2 only freezes and publishes `FrozenSubmission`. | Invalid input identity, duplicate completion, timeout, unsafe path or missing freeze evidence blocks publication. | connected freeze integration for Sprint 2; Runner/Checker evidence later. | freeze-only Evaluation is implemented; Runner, Checker, Aggregator and scoring are explicitly outside Sprint 2. |
| AC-04 | US-04 / deferred | OJ package supplies approved toolchain and fixed seed. | Reference solution and brute-force Oracle agree; mutation report identifies killed required mutants; approved Checker package is immutable. | Oracle disagreement, surviving required mutation, forbidden tool, missing seed, or absent approval blocks package publication. | future deterministic Runner/Checker integration and teacher E2E. | OJ Runner and Checker execution are outside Sprint 2. |
| AC-05 | US-05 / deferred | Approved VM template and declared probe policy are available. | Read-only probe returns schema-valid assertion facts tied to VM/template revision; any elevated check has separate approved declaration. | Unapproved privilege, state-changing probe, unreachable VM, malformed result, timeout, or missing evidence ends in explicit failed/cancelled state. | future Probe integration after the real KubeVirt lifecycle closes. | real KubeVirt lifecycle is Sprint 2 P0; Probe Runner is deferred. |
| AC-06 | US-06 / deferred | Active Work Lease, approved policy, and explicit build/configuration binding exist. | Request yields a diffable BuildKit/Ansible candidate tied to a Work revision; approved execution is rebuildable or rollback-capable. | Experiment target, arbitrary shell/source, prohibited privilege/mount/network, policy failure, missing approval, or failed recovery is rejected and audited. | future policy/Lease integration and research-user E2E. | WorkConfig and the research work-environment flow are outside Sprint 2. |
| AC-07 | US-07 / deferred | Scoped resource request and capacity provider binding exist. | Approval, quota, queue, Mock capacity allocation, lease, and expiry cleanup are observable and auditable. | Over-quota, absent approval, unavailable provider, duplicate request, expiry, or cleanup failure blocks allocation or records explicit remediation state. | future Resource/Lease integration and researcher/admin E2E. | Resource approval is outside Sprint 2 and Resource Service is not deployed. |
| AC-07-P1 | US-07 / P1 | A future preemption policy has been separately approved. | Low-priority GPU preemption is observable, fair, and auditable. | Any preemption without an approved policy is rejected. | E4 policy/failure-recovery evidence. | planned; P1 and excluded from the current Release Gate. |
| AC-08 | US-08 / P0 | Controlled inventory, fixed versions, and required administrative credentials are provided outside Git. | Preflight, non-destructive adoption, deploy, verify and atomic rollback emit sanitized reports tied to deployment identity; idempotent rerun is proven. | Missing prerequisite, migration/configuration conflict, stale report or recovery failure blocks release and retains the original diagnostic. | connected Ansible deploy/verify/recovery evidence. | implemented and proven on an older head; current-head reconcile is blocked by management connectivity. Destructive reset is excluded. |
| AC-09 | US-09 / P0 | Authenticated subject, scoped environment, healthy endpoint and active grant are present. | Authorized actor reaches only the registered endpoint through the Sprint 2 HTTP/OpenSSH Gateway; decision includes grant and endpoint revision. | Unauthorized actor, unhealthy endpoint, revoked/expired grant, target injection or Gateway outage denies new access fail closed. | connected Access/Gateway integration, revocation/expiry negatives and student E2E. | Access/Gateway are implemented; current-head connected denial and session-termination replay are pending. Tailnet is excluded. |
| AC-09-P1 | US-09 / P1 | Future project-level policy generation has separate approval. | Per-project policy compilation is revisioned and auditable. | Compilation/apply failure denies new access. | E4 policy evidence. | planned; P1 and excluded from the current Release Gate. |
| AC-10 | US-10 / P0 | Identified target build, connected run identities and role states are available. | Supported golden path waits for observable state, records run identity, and can be replayed through `cargo xtask demo replay`. | Missing identity, timeout, assertion failure or unavailable prerequisite fails the run and retains trace, screenshot and video. | connected Playwright multi-role and demo-replay evidence. | implementation exists and Fixture CI passes; real Keycloak replay and the passing same-identity report are pending. |
| AC-10-P1 | US-10 / P1 | Cross-browser visual baseline is separately approved. | Firefox/WebKit visual regression suite produces reviewable results. | Baseline mismatch retains visual evidence and blocks the applicable gate. | E4 cross-browser evidence. | planned; P1 and excluded from the current Release Gate. |

## Current P0 release-gate cross-check

The applicable Sprint 2 P0 rows cannot be marked complete until their connected
evidence is produced by the same build/deployment identity. Resource approval,
WorkConfig, Evaluation execution/scoring, Tailnet and Probe/Runner criteria are
deferred and cannot block this Sprint 2 slice. This document adds no Release
Gate pass and does not change any capability state to `done`.
