# Coverage Matrix

| Path | Positive | Negative | Current evidence | Required next evidence |
| --- | --- | --- | --- | --- |
| Service process | listener starts with explicit address | missing/invalid address fails | E1: `cargo xtask check`; Control service smoke | E2 process integration |
| Health contract | live/ready return versioned response | unknown route returns stable diagnostic | E1: unit tests and Control service smoke | E2 HTTP integration for all services |
| Request correlation | response contains request ID | malformed external ID policy not yet frozen | E1 unit | Contract decision and negative tests |
| Telemetry | JSON subscriber initializes | invalid filter fails | E1 build and code review | captured structured-log tests |
| Testable requirements baseline | US-01 to US-10 have stable 3C and acceptance IDs | missing target evidence, failed prerequisite, or weak evidence cannot be represented as done | E0 documented requirements matrix | mapped AC-01 to AC-10 P0 paths at their required E3/E4 levels |
| NATS v1 contract | typed Subject, CloudEvent and Owner semantics are generated | Subject/type/dataschema mismatch, duplicate/stale/gap sequence and protected payload are rejected | E1 `contracts` registry/schema tests | E2 real PostgreSQL plus JetStream validation |
| NATS delivery and quarantine | Owner publishes committed Outbox facts; durable Consumer processes declared purpose | duplicate/replay, stale/gap sequence, malformed envelope, retry exhaustion and consumer restart cause no silent business success | E0 design only | E2 publish/consume/replay/ack/quarantine integration evidence |
| Contracts generation | Rust types generate JSON Schema, isolated Public/Internal OpenAPI and Axios SDK | byte or file-set drift, mixed security surfaces and duplicate operation IDs fail | E1 `cargo xtask contracts check` plus TypeScript compile | consuming Handler and E2 integration evidence |
| EvaluationSpec v1 | OJ/Linux examples pass generated Schema and semantic validation | duplicate/missing/cyclic dependencies, unsafe paths, scoring boundary, LLM protected fields and tool allowlist are rejected | E1 `contracts` tests in current worktree | Runner contract suite and E2 Evaluation Service integration |
| PostgreSQL bootstrap and roles | provisioner creates NOLOGIN owners, constrained identities and fixed pools/search paths | `PUBLIC`/default privilege leakage, runtime DDL, history write, connection identity reuse and cross-domain access are denied | E2 Docker PostgreSQL integration in `persistence-sqlx` | service startup/readiness wiring |
| Schema startup policy | valid applied identity accepts traffic | missing/unknown/ahead/incomplete/checksum mismatch terminates; behind/unavailable is live but NotReady with 503 | E0 design only | E2 process and PostgreSQL diagnostic tests |
| Domain and release Migration execution | catalog-selected domains run once under global then domain locks | catalog/history mismatch, lock contention, different-release attempt, partial domain failure and unsupported down migration block release | E2 Docker PostgreSQL bootstrap/apply and report-hash coverage | crash recovery and reviewed forward-repair evidence |
| Outbox and audit projection | domain transaction atomically records its local Outbox event; Environment waits for JetStream persistence acknowledgement with event-ID deduplication; `shared_audit` is append-only | duplicate idempotency replay and runtime writes to `shared_audit` are denied | E2 Docker PostgreSQL transaction/permission plus Environment real JetStream replay/delivery coverage | shared audit projection, quarantine and replay/backfill remain unimplemented |
| EnvironmentLifecycle v1 and owner resolver | exhaustive desired/observed and operation matrices, atomic production first-create, idempotent SQLx writes, real JetStream Outbox/Inbox and exact Resource-Lease verification, durable Provider-step recovery, failed-phase/reset-target persistence, persistent timeout/cancel cleanup, explicit Provider binding and CA-verified owner resolution with strong ETag/database clock are implemented | invalid initial create, inactive/expired/mismatched Lease, incomplete idempotency or Provider-step identity, stale/conflicting revision, duplicate/stale/gap event, lease contention/loss, cleanup failure, invalid Provider observation, timeout diagnostic loss, untrusted/slow mTLS caller, clock skew, scope mismatch, deletion/expiry, shutdown signal failure and database/network outage fail closed | Local E2: Docker PostgreSQL 17, NATS JetStream 2.11 and real rustls mTLS integration; review feedback addressed, pending A re-review and D Verify | same-build D Verify, Resource/Access-owned responders, formal Provider/KubeVirt cleanup and E3 deployment evidence |
| Linux Nginx material contract | public template, candidate manifest and normal fact mapping agree | missing material, hash/marker mismatch, restricted content, oversized report, stopped service/not-listening mapping and site-mismatch mapping fail explicitly | E1 Python material validator in current worktree | B-approved SubmissionManifest/Probe profile, then E3 real KubeVirt VM evidence for normal and both negative paths |
| Agent state and Tool registry | explicit state path, capability-pinned low-risk Tool dispatch, bound timeout, cancellation, no implicit retry, idempotency identity propagation, validated output and payload-free attempt audit | illegal transitions, repair exhaustion, timeout, cancellation, unbound/duplicate/missing/version-risk-capability-conflict Tools and invalid input/output are rejected; closed implementation failure codes cannot forge Registry diagnostics or leak input markers; elevated/high-risk Tools fail closed | Partial E1 `agent-core` tests in current worktree | Role A decision for permissions and approval evidence; generic Tool service integration and AG-01b Fixture Backend; the separate Claude Code path exposes no Tools |
| Claude Code Agent process runtime | verified ProblemPackage input and pinned shell-free CLI invocation produce typed independent Environment/Evaluation candidates with hash-only runtime and usage audit; explicit process environment, unique HOME/XDG/tmp/workdir and bounded semaphore isolate/admit invocations; Agent store uses per-track fencing leases, heartbeat, durable cancellation, immediate checkpoint and terminal Outbox derivation | object/hash drift, classification failure/hard-deny, invalid/legacy bindings, model aliases, CLI mismatch, protected fields including normalized nested names, Markdown/non-JSON output, malformed/failed stream envelopes, invalid success identity, refusal/429/5xx, timeout/cancellation, budget overflow, provider stderr leakage, live double-claim and stale completion fail closed; exact terminal replay cannot invoke the runtime again | E1 `contracts` and `agent-service` runtime/process tests; E2 PostgreSQL 17 Migration, exact replay, 10-identical-request and 20-distinct-run contention across 4 workers, expired-lease attempt-2 recovery, one-track checkpoint retention and cross-worker durable cancellation passed 2026-07-15; the current isolated process path generated a real local Environment candidate with CLI `2.1.209` in 11.83 seconds at 35,321 microusd | run real Evaluation and dual-track tests; then HTTP/worker handoff, Control candidate projection, object-store reference, JetStream publisher/consumer, isolated Kubernetes Job, deployment Secret/Config injection and real provider E3; process isolation is not container isolation |
| GitHub governance | Milestones/Labels/Issues/branch rules and 15 P0 Ready items read back | insufficient scope was diagnosed and resolved in Issue #20 | E0 metadata | human review and continued Project maintenance |
| VM-01a KubeVirt, storage and Gateway infrastructure | run-scoped RWO, cross-worker RWX, hardware-KVM VM lifecycle, existing Gateway response and cleanup succeed | missing KVM/class/permission/Gateway, lifecycle failure or cleanup failure returns non-zero with a sanitized diagnostic | E3: `vm-01a-e3-20260713.md` at `48cda8de9fef775f7578c90ca879356979df2706` | Ansible replay, Access/Headscale, application paths and E4 release evidence |

## Infrastructure automation

| Capability | Owner | Entry point | Target evidence | Blocking condition |
| --- | --- | --- | --- | --- |
| Cluster preflight | A/D | `cargo xtask preflight --infra --env dev` | E1 then E3 replay | unresolved private config or network prerequisite |
| Kubernetes baseline | A | `cargo xtask deploy --infra --env dev --yes` on the router worktree | E3 guarded adopted-cluster reconciliation; second run is idempotent | package/version/bootstrap failure or baseline drift |
| Storage | A/D | deploy + verify | E3 RWO/RWX | unsafe formatting or mount/provision failure |
| KubeVirt/CDI | A/B/D | deploy + verify | E3 real VM lifecycle | missing KVM/CDI/scratch class |
| Gateway and policy | A/D | deploy + verify | E3 internal route | VIP not programmed or controller unavailable |
| Backup | A | `cargo xtask backup --infra --env dev --yes` on the router | E3 router execution with sanitized result | snapshot integrity failure |
| Ansible controller | D | Router Linux worktree via `ansible-rs` | E1/E2 lint and syntax plus E3 adopted-cluster baseline deploy/backup/TestFlight/idempotent replay | manifest identity failure; OIDC/governance and recovery remain #47/#2 release work |
| Direct VM access | documented DirectAccessGrant device/IP/port scope and Router-first dual-revision activation | missing/stale Headscale or Router receipt, inactive device, wrong endpoint, IP reuse, unsupported protocol and cross-user access remain blocked | E0: ACCESS-01a documentation and ADR 0001 | E1/E2 contract and dual-enforcement tests; E3 Headscale/Router evidence; E4 multi-device containment replay |
| Browser SSH/VNC proxy | documented Keycloak PKCE handoff, custom Guacamole extension and scoped credential boundary | handoff replay/expiry, invalid Access decision, credential disclosure and stale session are rejected | E0: ACCESS-01a documentation and ADR 0001 | E1/E2 token and extension tests; E3 deployed Guacamole path; E4 browser replay |
| Playwright role-project configuration | exactly four projects (`setup`, `teacher`, `student`, `platform-admin`), independent configured auth-state paths, retained failure artifacts, and aggregate E1 report | invalid project set, any `researcher` alias/test match/shared auth state, fixed sleeps, missing/invalid base URL, missing authentication setup, missing storage state, or zero runtime tests fail closed | E1 static configuration, aggregate-gate, and subprocess fail-fast contract tests; runtime E2E not executed | approved auth setup, a separately approved researcher project/auth state, real browser paths, authorization isolation, and E3/E4 evidence |
| KubeVirt/Evaluation | none | none | planned/blocked | E3/E4 evidence required by release gates |
