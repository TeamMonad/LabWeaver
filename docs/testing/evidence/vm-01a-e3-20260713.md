# VM-01a E3 KubeVirt and storage evidence — 2026-07-13

## Run identity

- Source commit under test: `48cda8de9fef775f7578c90ca879356979df2706`
- Run ID: `e3-20260713-1330`
- Evidence level: E3
- Scope: Issue #15 only; Headscale/Tailscale, AccessGrant, public exposure and
  Gateway configuration were not exercised.
- Raw machine-readable report: retained in the ignored controlled evidence
  location; it is not a Git artifact.

## Preconditions and read-only baseline

| Area | Result | Sanitized evidence |
| --- | --- | --- |
| Kubernetes API | PASS | readiness endpoint returned success |
| KVM | PASS | two schedulable Workers reported non-zero KVM capacity |
| KubeVirt/CDI | PASS | both control-plane resources were `Deployed` |
| StorageClass | PASS | `local-path` is default; `nfs-rwx` is present |
| Permissions | PASS | namespace-scoped VM and PVC creation returned `yes` |
| Gateway | PASS | existing Cilium Gateway was Programmed and its HTTPRoute was present |

## Controlled E3 results

| Check | Result | Sanitized evidence |
| --- | --- | --- |
| local-path RWO | PASS | PVC Bound; writer created and read its proof value |
| nfs-rwx RWX | PASS | PVC Bound; writer and a reader on different Workers read the same proof value |
| KubeVirt first start | PASS | fixed-digest container-disk VM reached Running with hardware KVM request |
| Console | PASS | `virtctl console` reported a successful connection; its bounded session ended at the expected external timeout |
| Stop and second start | PASS | VMI deletion completed after stop; the second VMI reached Running; second stop completed |
| Gateway request | PASS | existing internal route returned its expected response without configuration changes |
| Cleanup | PASS | run-scoped verification namespace deletion completed; a follow-up lookup returned no namespace |

## Verification and limitations

- `python -m py_compile scripts/preflight/kubevirt_preflight.py`: PASS.
- `python -m unittest discover -s tests/preflight`: PASS, 5 tests.
- E3 runner exit code: `0`.
- Earlier run IDs encountered a WaitForFirstConsumer ordering error and an
  unbounded console session. Both were cleaned up; the verifier now creates the
  consumer before waiting for Local Path binding and applies an external console
  time limit.
- This evidence proves the bounded VM-01a infrastructure path only. It does
  not prove an Ansible replay, Access/Headscale containment, application
  behavior, browser flow, recovery, or E4 release readiness.
