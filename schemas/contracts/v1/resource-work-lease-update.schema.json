{
  "$defs": {
    "ActorId": {
      "description": "Strongly typed UUIDv7 identifier for `ActorId`.",
      "type": "string"
    },
    "CourseId": {
      "description": "Strongly typed UUIDv7 identifier for `CourseId`.",
      "type": "string"
    },
    "EnvironmentId": {
      "description": "Strongly typed UUIDv7 identifier for `EnvironmentId`.",
      "type": "string"
    },
    "LeaseId": {
      "description": "Strongly typed UUIDv7 identifier for `LeaseId`.",
      "type": "string"
    },
    "Revision": {
      "description": "Monotonic aggregate revision. Zero is never a persisted revision.",
      "format": "uint64",
      "minimum": 0,
      "type": "integer"
    },
    "UtcTimestamp": {
      "description": "UTC timestamp serialized with a literal `Z` and millisecond precision.",
      "type": "string"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Resource-authoritative Lease update for an existing Work aggregate.\n\nEnvironment accepts this only from the Resource service mTLS identity after\nindependently resolving the exact active Lease. It extends eligibility but\ncannot change the immutable release, Provider, owner, course, or capacity\nbinding selected by the original handoff.",
  "properties": {
    "capacityBinding": {
      "type": "string"
    },
    "courseId": {
      "$ref": "#/$defs/CourseId"
    },
    "environmentId": {
      "$ref": "#/$defs/EnvironmentId"
    },
    "expiresAt": {
      "$ref": "#/$defs/UtcTimestamp"
    },
    "leaseId": {
      "$ref": "#/$defs/LeaseId"
    },
    "leaseRevision": {
      "$ref": "#/$defs/Revision"
    },
    "ownerActorId": {
      "$ref": "#/$defs/ActorId"
    },
    "traceId": {
      "type": "string"
    },
    "version": {
      "format": "uint8",
      "maximum": 255,
      "minimum": 0,
      "type": "integer"
    }
  },
  "required": [
    "version",
    "leaseId",
    "leaseRevision",
    "environmentId",
    "courseId",
    "ownerActorId",
    "capacityBinding",
    "expiresAt",
    "traceId"
  ],
  "title": "ResourceWorkLeaseUpdate",
  "type": "object"
}
