{
  "$defs": {
    "ActorId": {
      "description": "Strongly typed UUIDv7 identifier for `ActorId`.",
      "type": "string"
    },
    "CourseId": {
      "description": "Strongly typed UUIDv7 identifier for `CourseId`.",
      "type": "string"
    },
    "EnvironmentId": {
      "description": "Strongly typed UUIDv7 identifier for `EnvironmentId`.",
      "type": "string"
    },
    "LeaseId": {
      "description": "Strongly typed UUIDv7 identifier for `LeaseId`.",
      "type": "string"
    },
    "ObservedEnvironmentState": {
      "description": "Authoritative observed lifecycle state.",
      "enum": [
        "requested",
        "validating",
        "building",
        "provisioning",
        "ready",
        "stopping",
        "stopped",
        "updating",
        "expiring",
        "deleting",
        "deleted",
        "failed"
      ],
      "type": "string"
    },
    "ProjectId": {
      "description": "Strongly typed UUIDv7 identifier for `ProjectId`.",
      "type": "string"
    },
    "Revision": {
      "description": "Monotonic aggregate revision. Zero is never a persisted revision.",
      "format": "uint64",
      "minimum": 1,
      "type": "integer"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Minimal Resource-visible cleanup readback. Provider handles, endpoints and\nuser content never cross this boundary.",
  "properties": {
    "capacityBinding": {
      "type": "string"
    },
    "cleanupComplete": {
      "type": "boolean"
    },
    "courseId": {
      "anyOf": [
        {
          "$ref": "#/$defs/CourseId"
        },
        {
          "type": "null"
        }
      ]
    },
    "diagnosticCode": {
      "type": [
        "string",
        "null"
      ]
    },
    "environmentId": {
      "$ref": "#/$defs/EnvironmentId"
    },
    "leaseId": {
      "$ref": "#/$defs/LeaseId"
    },
    "leaseRevision": {
      "$ref": "#/$defs/Revision"
    },
    "observedState": {
      "$ref": "#/$defs/ObservedEnvironmentState"
    },
    "ownerActorId": {
      "$ref": "#/$defs/ActorId"
    },
    "projectId": {
      "$ref": "#/$defs/ProjectId"
    },
    "revision": {
      "$ref": "#/$defs/Revision"
    },
    "version": {
      "format": "uint8",
      "maximum": 255,
      "minimum": 0,
      "type": "integer"
    }
  },
  "required": [
    "version",
    "environmentId",
    "projectId",
    "ownerActorId",
    "leaseId",
    "leaseRevision",
    "capacityBinding",
    "revision",
    "observedState",
    "cleanupComplete"
  ],
  "title": "ResourceWorkCleanupStatus",
  "type": "object"
}
