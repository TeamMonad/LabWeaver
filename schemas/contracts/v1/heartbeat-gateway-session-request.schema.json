{
  "$defs": {
    "Revision": {
      "description": "Monotonic aggregate revision. Zero is never a persisted revision.",
      "format": "uint64",
      "minimum": 0,
      "type": "integer"
    },
    "UtcTimestamp": {
      "description": "UTC timestamp serialized with a literal `Z` and millisecond precision.",
      "type": "string"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Revision-checked heartbeat emitted only by the owning Gateway connection.",
  "properties": {
    "connectionId": {
      "type": "string"
    },
    "expectedRevision": {
      "$ref": "#/$defs/Revision"
    },
    "gatewayIdentity": {
      "type": "string"
    },
    "observedAt": {
      "$ref": "#/$defs/UtcTimestamp"
    }
  },
  "required": [
    "gatewayIdentity",
    "connectionId",
    "expectedRevision",
    "observedAt"
  ],
  "title": "HeartbeatGatewaySessionRequest",
  "type": "object"
}
