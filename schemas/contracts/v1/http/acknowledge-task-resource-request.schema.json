{
  "$defs": {
    "Revision": {
      "description": "Monotonic aggregate revision. Zero is never a persisted revision.",
      "format": "uint64",
      "minimum": 1,
      "type": "integer"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Revision fence for acknowledging a task resource handoff.",
  "properties": {
    "executionNamespace": {
      "description": "Exact provider namespace in which this task will execute.",
      "type": "string"
    },
    "expectedClaimRevision": {
      "$ref": "#/$defs/Revision"
    },
    "expectedLeaseRevision": {
      "$ref": "#/$defs/Revision"
    }
  },
  "required": [
    "expectedClaimRevision",
    "expectedLeaseRevision",
    "executionNamespace"
  ],
  "title": "AcknowledgeTaskResourceRequest",
  "type": "object"
}
