{
  "$defs": {
    "CourseId": {
      "description": "Strongly typed UUIDv7 identifier for `CourseId`.",
      "type": "string"
    },
    "ProblemPackageUploadFile": {
      "additionalProperties": false,
      "properties": {
        "mediaType": {
          "type": "string"
        },
        "path": {
          "type": "string"
        },
        "sizeBytes": {
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "path",
        "sizeBytes",
        "mediaType"
      ],
      "type": "object"
    },
    "ProblemPackageUploadTarget": {
      "description": "Short-lived, per-object upload authority returned only by session creation.",
      "properties": {
        "expiresAt": {
          "$ref": "#/$defs/UtcTimestamp"
        },
        "path": {
          "type": "string"
        },
        "requiredHeaders": {
          "additionalProperties": {
            "type": "string"
          },
          "type": "object"
        },
        "uploadUrl": {
          "type": "string"
        }
      },
      "required": [
        "path",
        "uploadUrl",
        "requiredHeaders",
        "expiresAt"
      ],
      "type": "object"
    },
    "ProjectId": {
      "description": "Strongly typed UUIDv7 identifier for `ProjectId`.",
      "type": "string"
    },
    "Revision": {
      "description": "Monotonic aggregate revision. Zero is never a persisted revision.",
      "format": "uint64",
      "minimum": 1,
      "type": "integer"
    },
    "UploadSessionId": {
      "description": "Strongly typed UUIDv7 identifier for `UploadSessionId`.",
      "type": "string"
    },
    "UtcTimestamp": {
      "description": "UTC timestamp serialized with a literal `Z` and millisecond precision.",
      "type": "string"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "properties": {
    "courseId": {
      "anyOf": [
        {
          "$ref": "#/$defs/CourseId"
        },
        {
          "type": "null"
        }
      ]
    },
    "expiresAt": {
      "$ref": "#/$defs/UtcTimestamp"
    },
    "files": {
      "items": {
        "$ref": "#/$defs/ProblemPackageUploadFile"
      },
      "type": "array"
    },
    "id": {
      "$ref": "#/$defs/UploadSessionId"
    },
    "projectId": {
      "$ref": "#/$defs/ProjectId"
    },
    "revision": {
      "$ref": "#/$defs/Revision"
    },
    "uploadTargets": {
      "items": {
        "$ref": "#/$defs/ProblemPackageUploadTarget"
      },
      "type": "array"
    }
  },
  "required": [
    "id",
    "projectId",
    "revision",
    "files",
    "uploadTargets",
    "expiresAt"
  ],
  "title": "ProblemPackageUploadSession",
  "type": "object"
}
