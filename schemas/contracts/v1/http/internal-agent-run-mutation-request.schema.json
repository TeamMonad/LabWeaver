{
  "$defs": {
    "CourseId": {
      "description": "Strongly typed UUIDv7 identifier for `CourseId`.",
      "type": "string"
    },
    "ProjectId": {
      "description": "Strongly typed UUIDv7 identifier for `ProjectId`.",
      "type": "string"
    },
    "Revision": {
      "description": "Monotonic aggregate revision. Zero is never a persisted revision.",
      "format": "uint64",
      "minimum": 1,
      "type": "integer"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Revision precondition for Control-to-Agent cancellation and retry mutations.",
  "properties": {
    "courseId": {
      "anyOf": [
        {
          "$ref": "#/$defs/CourseId"
        },
        {
          "type": "null"
        }
      ]
    },
    "expectedRevision": {
      "$ref": "#/$defs/Revision",
      "description": "Exact Agent-owned run revision observed by Control."
    },
    "projectId": {
      "$ref": "#/$defs/ProjectId",
      "description": "Exact Project authority that owns the target run."
    }
  },
  "required": [
    "projectId",
    "expectedRevision"
  ],
  "title": "InternalAgentRunMutationRequest",
  "type": "object"
}
