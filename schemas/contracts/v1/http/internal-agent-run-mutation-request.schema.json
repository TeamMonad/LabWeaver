{
  "$defs": {
    "Revision": {
      "description": "Monotonic aggregate revision. Zero is never a persisted revision.",
      "format": "uint64",
      "minimum": 0,
      "type": "integer"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Revision precondition for Control-to-Agent cancellation and retry mutations.",
  "properties": {
    "expectedRevision": {
      "$ref": "#/$defs/Revision",
      "description": "Exact Agent-owned run revision observed by Control."
    }
  },
  "required": [
    "expectedRevision"
  ],
  "title": "InternalAgentRunMutationRequest",
  "type": "object"
}
