{
  "$defs": {
    "Revision": {
      "description": "Monotonic aggregate revision. Zero is never a persisted revision.",
      "format": "uint64",
      "minimum": 1,
      "type": "integer"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Revision-fenced removal of one Project membership.",
  "properties": {
    "expectedRevision": {
      "$ref": "#/$defs/Revision"
    },
    "reason": {
      "type": "string"
    }
  },
  "required": [
    "expectedRevision",
    "reason"
  ],
  "title": "RemoveProjectMembershipRequest",
  "type": "object"
}
