{
  "$defs": {
    "ActorId": {
      "description": "Strongly typed UUIDv7 identifier for `ActorId`.",
      "type": "string"
    },
    "CourseId": {
      "description": "Strongly typed UUIDv7 identifier for `CourseId`.",
      "type": "string"
    },
    "EndpointId": {
      "description": "Strongly typed UUIDv7 identifier for `EndpointId`.",
      "type": "string"
    },
    "EnvironmentAccessSubjectKind": {
      "enum": [
        "owner",
        "course_teacher"
      ],
      "type": "string"
    },
    "EnvironmentId": {
      "description": "Strongly typed UUIDv7 identifier for `EnvironmentId`.",
      "type": "string"
    },
    "Revision": {
      "description": "Monotonic aggregate revision. Zero is never a persisted revision.",
      "format": "uint64",
      "minimum": 0,
      "type": "integer"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Fail-closed Access Service request for exact endpoint eligibility.",
  "properties": {
    "actorId": {
      "$ref": "#/$defs/ActorId"
    },
    "courseId": {
      "$ref": "#/$defs/CourseId"
    },
    "endpointIds": {
      "items": {
        "$ref": "#/$defs/EndpointId"
      },
      "type": "array"
    },
    "environmentId": {
      "$ref": "#/$defs/EnvironmentId"
    },
    "expectedRevision": {
      "$ref": "#/$defs/Revision"
    },
    "subjectKind": {
      "$ref": "#/$defs/EnvironmentAccessSubjectKind"
    }
  },
  "required": [
    "environmentId",
    "courseId",
    "actorId",
    "subjectKind",
    "expectedRevision",
    "endpointIds"
  ],
  "title": "EnvironmentEndpointEligibilityRequest",
  "type": "object"
}
