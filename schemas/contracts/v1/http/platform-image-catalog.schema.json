{
  "$defs": {
    "PlatformImageEntry": {
      "additionalProperties": false,
      "description": "One pinned platform image identity as persisted by the Agent authority.",
      "properties": {
        "binding": {
          "type": "string"
        },
        "catalogId": {
          "$ref": "#/$defs/PlatformImageId"
        },
        "kind": {
          "$ref": "#/$defs/PlatformImageKind"
        },
        "mediaType": {
          "type": "string"
        },
        "pinnedAt": {
          "$ref": "#/$defs/UtcTimestamp"
        },
        "repinGeneration": {
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "resolvedDigest": {
          "type": "string"
        },
        "sizeBytes": {
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "sourceReference": {
          "type": "string"
        },
        "status": {
          "$ref": "#/$defs/PlatformImageStatus"
        },
        "trustRevision": {
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "updatedAt": {
          "$ref": "#/$defs/UtcTimestamp"
        }
      },
      "required": [
        "catalogId",
        "kind",
        "binding",
        "sourceReference",
        "resolvedDigest",
        "mediaType",
        "sizeBytes",
        "status",
        "trustRevision",
        "repinGeneration",
        "pinnedAt",
        "updatedAt"
      ],
      "type": "object"
    },
    "PlatformImageId": {
      "description": "Strongly typed UUIDv7 identifier for `PlatformImageId`.",
      "type": "string"
    },
    "PlatformImageKind": {
      "description": "Reviewed platform image kinds.",
      "enum": [
        "container",
        "virtual_machine"
      ],
      "type": "string"
    },
    "PlatformImageStatus": {
      "description": "Catalog lifecycle status.",
      "enum": [
        "active",
        "disabled"
      ],
      "type": "string"
    },
    "UtcTimestamp": {
      "description": "UTC timestamp serialized with a literal `Z` and millisecond precision.",
      "type": "string"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Catalog listing returned by the Agent authority and the administrator gateway.",
  "properties": {
    "entries": {
      "items": {
        "$ref": "#/$defs/PlatformImageEntry"
      },
      "type": "array"
    }
  },
  "required": [
    "entries"
  ],
  "title": "PlatformImageCatalog",
  "type": "object"
}
