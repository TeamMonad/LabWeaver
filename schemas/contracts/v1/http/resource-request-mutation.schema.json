{
  "$defs": {
    "Revision": {
      "description": "Monotonic aggregate revision. Zero is never a persisted revision.",
      "format": "uint64",
      "minimum": 0,
      "type": "integer"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Revision-fenced reason-bearing Resource mutation.",
  "properties": {
    "expectedRevision": {
      "$ref": "#/$defs/Revision"
    },
    "reason": {
      "type": "string"
    }
  },
  "required": [
    "expectedRevision",
    "reason"
  ],
  "title": "ResourceRequestMutation",
  "type": "object"
}
