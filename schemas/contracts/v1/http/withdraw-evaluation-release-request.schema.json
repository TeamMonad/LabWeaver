{
  "$defs": {
    "DiagnosticCode": {
      "description": "Stable machine-readable diagnostic code.\n\nConsumers must treat an unknown `LW_*` code as blocking. The newtype is intentionally open so\nadditive diagnostics do not force a wire-version change.",
      "type": "string"
    },
    "Revision": {
      "description": "Monotonic aggregate revision. Zero is never a persisted revision.",
      "format": "uint64",
      "minimum": 0,
      "type": "integer"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Revision-fenced append-only Evaluation release withdrawal.",
  "properties": {
    "expectedRevision": {
      "$ref": "#/$defs/Revision"
    },
    "reasonCode": {
      "$ref": "#/$defs/DiagnosticCode"
    }
  },
  "required": [
    "expectedRevision",
    "reasonCode"
  ],
  "title": "WithdrawEvaluationReleaseRequest",
  "type": "object"
}
