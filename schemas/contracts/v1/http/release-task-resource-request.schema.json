{
  "$defs": {
    "Revision": {
      "description": "Monotonic aggregate revision. Zero is never a persisted revision.",
      "format": "uint64",
      "minimum": 1,
      "type": "integer"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Revision fence for releasing a task resource reservation.",
  "properties": {
    "expectedClaimRevision": {
      "$ref": "#/$defs/Revision"
    },
    "expectedLeaseRevision": {
      "$ref": "#/$defs/Revision"
    }
  },
  "required": [
    "expectedClaimRevision",
    "expectedLeaseRevision"
  ],
  "title": "ReleaseTaskResourceRequest",
  "type": "object"
}
