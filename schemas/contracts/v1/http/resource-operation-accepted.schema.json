{
  "$defs": {
    "LeaseId": {
      "description": "Strongly typed UUIDv7 identifier for `LeaseId`.",
      "type": "string"
    },
    "ResourceRequestId": {
      "description": "Strongly typed UUIDv7 identifier for `ResourceRequestId`.",
      "type": "string"
    },
    "Revision": {
      "description": "Monotonic aggregate revision. Zero is never a persisted revision.",
      "format": "uint64",
      "minimum": 1,
      "type": "integer"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Stable response returned when Resource accepts an asynchronous allocation mutation.",
  "properties": {
    "leaseId": {
      "anyOf": [
        {
          "$ref": "#/$defs/LeaseId"
        },
        {
          "type": "null"
        }
      ]
    },
    "requestId": {
      "$ref": "#/$defs/ResourceRequestId"
    },
    "revision": {
      "$ref": "#/$defs/Revision"
    },
    "statusUrl": {
      "type": "string"
    }
  },
  "required": [
    "requestId",
    "revision",
    "statusUrl"
  ],
  "title": "ResourceOperationAccepted",
  "type": "object"
}
