{
  "$defs": {
    "ActorId": {
      "description": "Strongly typed UUIDv7 identifier for `ActorId`.",
      "type": "string"
    },
    "BuildRequestId": {
      "description": "Strongly typed UUIDv7 identifier for `BuildRequestId`.",
      "type": "string"
    },
    "CourseId": {
      "description": "Strongly typed UUIDv7 identifier for `CourseId`.",
      "type": "string"
    },
    "InternalAgentBuildState": {
      "description": "Agent-owned durable state used as an optimistic precondition for build cancellation.",
      "enum": [
        "requested",
        "running",
        "succeeded",
        "failed",
        "cancelled"
      ],
      "type": "string"
    },
    "ProjectId": {
      "description": "Strongly typed UUIDv7 identifier for `ProjectId`.",
      "type": "string"
    },
    "Revision": {
      "description": "Monotonic aggregate revision. Zero is never a persisted revision.",
      "format": "uint64",
      "minimum": 1,
      "type": "integer"
    },
    "UtcTimestamp": {
      "description": "UTC timestamp serialized with a literal `Z` and millisecond precision.",
      "type": "string"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Control-to-Agent build cancellation command authenticated by the service JWT boundary.",
  "properties": {
    "actorId": {
      "$ref": "#/$defs/ActorId"
    },
    "buildRequestId": {
      "$ref": "#/$defs/BuildRequestId"
    },
    "courseId": {
      "anyOf": [
        {
          "$ref": "#/$defs/CourseId"
        },
        {
          "type": "null"
        }
      ]
    },
    "expectedRevision": {
      "$ref": "#/$defs/Revision"
    },
    "expectedState": {
      "$ref": "#/$defs/InternalAgentBuildState"
    },
    "projectId": {
      "$ref": "#/$defs/ProjectId"
    },
    "requestedAt": {
      "$ref": "#/$defs/UtcTimestamp"
    }
  },
  "required": [
    "projectId",
    "buildRequestId",
    "expectedState",
    "expectedRevision",
    "actorId",
    "requestedAt"
  ],
  "title": "InternalAgentBuildCancellationRequest",
  "type": "object"
}
