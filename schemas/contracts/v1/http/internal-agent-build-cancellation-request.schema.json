{
  "$defs": {
    "ActorId": {
      "description": "Strongly typed UUIDv7 identifier for `ActorId`.",
      "type": "string"
    },
    "BuildRequestId": {
      "description": "Strongly typed UUIDv7 identifier for `BuildRequestId`.",
      "type": "string"
    },
    "CourseId": {
      "description": "Strongly typed UUIDv7 identifier for `CourseId`.",
      "type": "string"
    },
    "InternalAgentBuildState": {
      "description": "Agent-owned durable state used as an optimistic precondition for build cancellation.",
      "enum": [
        "requested",
        "running",
        "succeeded",
        "failed",
        "cancelled"
      ],
      "type": "string"
    },
    "Revision": {
      "description": "Monotonic aggregate revision. Zero is never a persisted revision.",
      "format": "uint64",
      "minimum": 0,
      "type": "integer"
    },
    "Sha256Digest": {
      "description": "Canonical lowercase SHA-256 digest.",
      "type": "string"
    },
    "UtcTimestamp": {
      "description": "UTC timestamp serialized with a literal `Z` and millisecond precision.",
      "type": "string"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Control-to-Agent build cancellation command carried only over allowlisted mTLS.",
  "properties": {
    "actorId": {
      "$ref": "#/$defs/ActorId"
    },
    "authoritySanUri": {
      "description": "Exact verified Control URI SAN; the Agent compares it with the mTLS peer principal.",
      "type": "string"
    },
    "buildRequestId": {
      "$ref": "#/$defs/BuildRequestId"
    },
    "commandSha256": {
      "$ref": "#/$defs/Sha256Digest"
    },
    "courseId": {
      "$ref": "#/$defs/CourseId"
    },
    "expectedRevision": {
      "$ref": "#/$defs/Revision"
    },
    "expectedState": {
      "$ref": "#/$defs/InternalAgentBuildState"
    },
    "requestedAt": {
      "$ref": "#/$defs/UtcTimestamp"
    }
  },
  "required": [
    "courseId",
    "buildRequestId",
    "commandSha256",
    "expectedState",
    "expectedRevision",
    "actorId",
    "authoritySanUri",
    "requestedAt"
  ],
  "title": "InternalAgentBuildCancellationRequest",
  "type": "object"
}
