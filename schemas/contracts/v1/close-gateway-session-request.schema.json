{
  "$defs": {
    "Revision": {
      "description": "Monotonic aggregate revision. Zero is never a persisted revision.",
      "format": "uint64",
      "minimum": 0,
      "type": "integer"
    },
    "UtcTimestamp": {
      "description": "UTC timestamp serialized with a literal `Z` and millisecond precision.",
      "type": "string"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Terminal receipt from the Gateway. Terminal content is never accepted.",
  "properties": {
    "closedAt": {
      "$ref": "#/$defs/UtcTimestamp"
    },
    "connectionId": {
      "type": "string"
    },
    "expectedRevision": {
      "$ref": "#/$defs/Revision"
    },
    "gatewayIdentity": {
      "type": "string"
    },
    "reasonCode": {
      "type": "string"
    }
  },
  "required": [
    "gatewayIdentity",
    "connectionId",
    "expectedRevision",
    "closedAt",
    "reasonCode"
  ],
  "title": "CloseGatewaySessionRequest",
  "type": "object"
}
