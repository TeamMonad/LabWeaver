# LabWeaver 开发约定

依据 36 题产品选择与已批准的 v3 实施方案。产品目标不代表已验证能力；当前进度和测试结果放 Issue/PR。服务配置与部署说明放相应文档。

## 产品与服务

LabWeaver 是 Agent 原生的教学与科研通用工作环境平台。长期维护，不再受旧课程日期或 Sprint 冻结限制。以用户完成教学、科研、环境配置和资源管理为目标，不以证明报告数量衡量完成度。

保留独立构建/部署的六个业务服务：Control、Access、Environment、Agent、Evaluation、Resource。不得未经产品决定合并为单体或删除 Resource/Evaluation。

核心产品包括容器/KubeVirt 环境、独立于课程的 Work、Agent 生成配置与镜像及配置排障、统一评测、资源审批、真实 GPU 与计费。当前执行后端为 Kubernetes/KubeVirt；Slurm 为未来方向，不为此保留空运行实现。

Project 是基本归属，Course 是教学关联。Experiment 与 Work 共享环境生命周期；用途、执行后端、资源规格和权限策略通过组合表达，不复制整套实现。

## 职责

| 角色 | 责任 |
|---|---|
| A / `@2018wzh` | 架构、Control、Access、Resource、计费、核心契约和集成 |
| B / `@zeyi2` | Agent、Environment、Evaluation、执行、收集和评测 |
| C / `@yingxvemiao` | 前端、用户流程、管理员配置体验 |
| D / `@Nova-Lciop-J` | 测试、DevOps、部署文档和行为验证 |

CODEOWNERS 负责路由评审，不代表业务审批或自动安全认证。核心权限、评分、计费、迁移和清理边界由相关核心负责人评审；D 验证受影响行为。旧 Issue 编号、候选冻结和操作台账不成为永久职责。

## 架构

每个领域只有一个权威业务实现。允许共享具体函数、结构体、模块和基础设施实现，不为保持“分层完整”增加 Adapter/Provider/Registry。

只有真实替换边界或多个当前实现需要接口。没有消费者的未来兼容层、模拟产品、占位服务和空实现应移除，而不是强迫现有业务经过它们。

组合入口只加载配置、组装依赖、启动处理循环和停止资源。`--mode` 选择进程角色可以保留，但不得在各角色中复制生命周期。跨平台、测试和生产共享正确性语义，昂贵诊断可选。

PostgreSQL 保存业务事实，NATS 服务真实异步任务与事件。普通同步读取不为架构形式绕经消息队列。保留幂等、条件更新、任务代次、取消和资源回收。

允许同一 PostgreSQL 数据库中的有限跨 Schema 应用事务，但须有明确唯一协调入口、领域 Owner 实现和受限权限。跨进程 RPC 不自动属于同一事务。不得持锁等待 LLM、构建、集群调用或人工审批，也不新增通用分布式事务框架。

同一 Kubernetes 资源/字段有一个明确管理者。Resource 管批准、分配授权、用量和费用；Environment 管环境实际运行；Evaluation 管评测任务和成绩，互不维护第二套相同权威状态。

## Agent、实验与安全

Agent 在隔离临时上下文生成配置和镜像、构建和验证。共享实验包由教师一次批准；私人 Work 可在用户/管理员预授权范围内配置，超范围操作明确确认。

配置脚本可以执行，但必须在获授权范围和对应执行隔离中。Agent 不持集群管理员、全仓库或宿主机权限，不把自然语言当成平台管理命令。Claude Code 暂为唯一后端，专有协议留在薄适配器。

无法满足目标明确失败；可以保留失败草稿，不得静默替换为更弱目标或固定成功。LLM 不写确定性数值成绩。冻结文件与实时 Probe 明确区分输入版本、运行实例和观察时间。

学生可以修改实验要求的运行实例、源码和 guest 配置；不能修改共享基线、隐藏评测规则或其他人的资源。“实验不可变”不能阻止 xv6、系统配置和 CTF 的合法实验操作。

校内用户也可能运行不可信程序。按场景允许 guest root、实验网络和特权研究，但不全局放宽控制面、跨租户、平台凭据和资源限制。可能攻击隔离层本身的实验使用专用边界。

身份、权限、资源限额和版本完整性必须真实实现。禁止以固定 principal 伪装调用方鉴别，以 no-op 假装扫描，以空测试返回 Ok 假装验证。开发/测试/生产不得切换正确性标准。

Secret、访问 Token、私钥和未授权用户内容不进入 Git、镜像或普通日志。保留正常诊断上下文，按权限访问详细日志；不能为“只准 hash”让故障无法诊断。

## 计费与用户体验

计费是 Resource 内的真实业务模块；用量区间、费率版本、入账和必要调整记录必须保留，不属于开发证明链。重复/乱序事件不能重复计费，未知计量不伪装为零，停止请求不等于资源已释放。

费率、费用单位和是否真实支付由运营配置明确；不默认接支付网关或对真实用户扣款。GPU 不可用时明确拒绝/等待，不换成 Mock 宣称成功。

普通用户不需要理解 Provider、NATS、Kubernetes 对象、内部 revision 或部署报告才能完成任务。管理员获得真实可用的高级表单、说明、校验和影响提示。YAML 可作高级入口，不能是唯一入口或另一套配置真源。

长任务提供可查询状态、取消、结果与可读错误，进度未知不制造百分比。Work 重启或不可逆变更必须提前说明。刷新和重试不得导致重复资源或重复账单。

## 开发、测试与交付

开始修改前读取受影响源码、当前分支与工作树，保护已有未提交改动。大规模重构在独立分支完成，按纵向业务切片更新契约、调用方、实现、配置、测试和文档。

当前无外部消费者，可破坏性更新全仓调用者并清理旧版本，不保留无用 v1/v2 兼容外壳。未来真实数据投入后重新审视迁移要求，不将本次清空许可永久化。

优先运行受影响检查；共享契约、权限、持久化和计费扩大验证，真实执行后端变化做对应集成测试。关键用户旅程用真实业务后端；只在外部依赖边界 Mock，不维护第二套前端业务状态机。

常用现有命令（包名按实际工作范围选择）：

```sh
cargo fmt --all -- --check
cargo check -p <package> --locked
cargo test -p <package> --locked
cargo clippy -p <package> --all-targets -- -D warnings
pnpm --dir web lint
pnpm --dir web typecheck
pnpm --dir web test
```

以上是命令形态示例；含 `<package>` 的命令需替换真实包名。以当前清单和脚本为准，不把未创建命令写成已可用入口。必要的全 workspace 检查保留，但不要求每个局部改动触发全平台重放。

删除仅锁定类型名、任务标题、旧文件路径或架构层存在的测试；保留输入、权限、状态、评分、计费、恢复和清理的行为保护。生成代码从单一真源产生，不手工修改生成 SDK 来降低行数。

平台发布只检查实际构建、配置、迁移、依赖与受影响用户行为。不要求固定镜像数量、E0–E4、同一 Run ID 证明包、approval hash 或覆盖矩阵。

任务和状态放 Issue/PR；文档保留现行产品、架构、格式、开发与运维说明。完成报告写改了什么、实际测试结果和未验证风险，不要求另建状态数据库。

## 本次实施范围与协作

Issue #180 使用单一分支和 Draft PR，允许全仓接口硬切及跨模块实施，不拆子 Issue。主模型负责架构、契约协调与集成；所有代码、测试和生成器修改由 gpt-5.6-luna、max 子 agent 执行，最多三个并行且文件所有权不重叠。共享契约和迁移由单个子 agent 串行修改。

当前 ROLE 的 A 负责全仓实施协调，B/C/D 保留相应人类评审责任。禁止自行批准、合并或发布。PR 关联 Issue，目标 develop，更新前 rebase 最新 origin/develop，并显式请求 B/C/D Reviewer。

本轮只做代码与本地验证，不执行远端部署、基础设施清理、真实扣款。独占 GPU、容器时间片和 VM vGPU 均为实现范围；真实设备能力需后续实机验证。计费先核算、预算提醒与调整，不默认预算强停。

内部服务身份采用 Keycloak 服务账户、JWT issuer/audience/期限/签名与调用权限校验，用户范围授权另行检查；服务端 TLS 保护令牌传输，不使用固定 principal 或假 mTLS。

保护用户无关改动；文档和脚本使用项目相对路径与跨平台命令。优先增量验证，构建锁等待。不得以测试数量或净删行数替代正确性。
