# LabWeaver Web 环境变量模板
# 复制为 .env.development 或 .env.production 后填入实际值

# API 网关 origin/base（生成路径已包含 /api/v1，不得在此重复）
VITE_API_BASE_URL=/
# Portal deployments use the HttpOnly BFF session. Set bearer only for approved direct API clients.
VITE_API_AUTH_MODE=bff

# OIDC / Keycloak 配置
VITE_OIDC_AUTHORITY=https://keycloak.labweaver.local/realms/labweaver
VITE_OIDC_CLIENT_ID=labweaver-web
VITE_OIDC_REDIRECT_URI=http://localhost:5173/auth/callback
VITE_OIDC_POST_LOGOUT_REDIRECT_URI=http://localhost:5173/

# 平台名称
VITE_APP_TITLE=LabWeaver 智织实验云

# 数据运行模式：live（默认，连接真实后端）或 fixture（仅开发/测试）
VITE_DATA_MODE=live
