---
# This lock is intentionally public. The matching machine identity lives only
# in the root-owned controller locator configured outside the repository.
schema_version: 1
# Comma-separated, explicit controller identities. Each still requires a
# root-owned locator whose machine_id matches the executing Linux host.
# Docker Desktop's Linux controller image is a local, non-release validation
# controller. It may run only the read-only local preflight/replay plan unless
# a separate Sprint-end acceptance Issue authorizes connected writes.
# v1-cp-63 is the explicitly adopted Sprint-2 acceptance controller for the
# retained v1 cluster; its root-only identity locator must still match the
# host machine-id before any connected playbook can start.
approved_controller_ids: edge-router,wsl-a-controller,docker-desktop-controller,v1-cp-63
ansible_core_version: 2.18.6
ansible_lint_version: 25.6.1
python_kubernetes_version: 34.1.0
python_jsonschema_version: 4.24.0
helm_version: 3.21.3
helm_linux_amd64_sha256: 35da09ba0716fc7c3cd63b6b31ee380a9c7662e95f29ab0e4ae962420afd315b
