---
- import_playbook: 00-preflight.yml
  vars:
    labweaver_preflight_scope: platform-reset

- name: Reset pre-release LabWeaver state and deploy the Sprint 2 baseline
  hosts: control_plane
  gather_facts: false
  become: true
  roles: [platform_reset]
