---
- name: Run the Harbor route preflight
  import_playbook: 00-preflight.yml
  vars:
    labweaver_preflight_scope: platform-harbor-route

- name: Reconcile the Harbor Gateway route
  hosts: control_plane
  gather_facts: false
  roles: [platform_harbor_route]
