---
- import_playbook: 00-preflight.yml
  vars:
    labweaver_preflight_scope: private-sigstore
- name: Run identity-bound Private Sigstore disaster recovery
  hosts: control_plane
  gather_facts: false
  roles:
    - role: private_sigstore_lifecycle
      private_sigstore_lifecycle_action: disaster-recovery
