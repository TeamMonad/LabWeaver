---
- import_playbook: 00-preflight.yml
- name: Rotate Private Sigstore public trust metadata
  hosts: control_plane
  gather_facts: false
  roles:
    - role: private_sigstore_lifecycle
      private_sigstore_lifecycle_action: rotate
