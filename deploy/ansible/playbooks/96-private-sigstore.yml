---
- import_playbook: 00-preflight.yml
  vars:
    labweaver_preflight_scope: private-sigstore

- name: Reconcile Private Sigstore on an adopted LabWeaver cluster
  hosts: control_plane
  gather_facts: false
  roles: [private_sigstore]
