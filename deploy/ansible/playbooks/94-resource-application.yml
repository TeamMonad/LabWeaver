---
- import_playbook: 00-preflight.yml
  vars:
    labweaver_preflight_scope: platform-application
    labweaver_preflight_validate_remote_hosts: false

- name: Deploy the independently reviewed Resource authority profile
  hosts: localhost
  connection: local
  gather_facts: false
  become: true
  roles: [resource_application]
